Minimal namespace setup
setup_vm_test.sh

Build
gcc -O2 proto_demo.c -o proto_demo

Terminal 1 (receiver)
sudo ip netns exec nsB bash

Inside nsB: ………….

Terminal 2 (sender)
sudo ip netns exec nsA bash

Inside nsA: ……………….

Get MAC of vethB:

ip netns exec nsB ip link show vethB

Suppose:

8a:bb:cc:dd:ee:ff

Run:

./proto_demo tx tun0 vethA 8a:bb:cc:dd:ee:ff
Test

From nsA:

ping 10.20.0.1

Expected:

ping works
TX messages appear
RX messages appear