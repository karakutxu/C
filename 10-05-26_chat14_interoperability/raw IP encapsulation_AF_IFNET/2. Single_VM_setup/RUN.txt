
+----------------------+
| nsA                  |
|                      |
| tun0 = 10.10.0.1     |
| vethA = 192.168.100.1|
|                      |
| proto_demo tx        |
+----------------------+
          |
          | UDP/IP
          |
+----------------------+
| nsB                  |
|                      |
| tun0 = 10.20.0.1     |
| vethB = 192.168.100.2|
|                      |
| proto_demo rx        |
+----------------------+

Compile
gcc proto_demo.c -o proto_demo
Run Receiver
Terminal 1
sudo ip netns exec nsB bash

Then:

./proto_demo rx tun0 dummy
Run Sender
Terminal 2
sudo ip netns exec nsA bash

Then:

./proto_demo tx tun0 192.168.100.2

IMPORTANT:

192.168.100.2 = UNDERLAY transport IP

NOT:

10.20.0.1
Generate Overlay Traffic
Terminal 3
sudo ip netns exec nsA bash

Then:

ping 10.20.0.1
Expected Result
nsA
TX: 10.10.0.1 -> 10.20.0.1 len=84
nsB
RX: 10.10.0.1 -> 10.20.0.1 len=84
Why this demo is valuable

This VM test now accurately models:

VM demo	Real deployment
veth pair	physical Ethernet
namespace	separate host
UDP socket	routed IP transport
tun0	overlay interface
proto_demo	protocol daemon

This is a very strong prototype for your migration effort.

Most important architectural insight

You are now very close to:

OpenVPN/WireGuard-style overlay architecture

which is:

mature
scalable
routable
interoperable
Linux-native

and aligns much more closely with the legacy code you discovered.